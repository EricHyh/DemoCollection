<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:textColor="#000000"/>


    <com.laser.message.ui.widget.ProgressButton
        android:id="@+id/pb_progress"
        android:layout_width="56dp"
        android:layout_height="30dp"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="16dp"
        android:layout_marginStart="16dp"


        app:backgroundColor="@color/progress_btn_background_color"
        app:backgroundTextColor="@color/progress_btn_text_background"
        app:corner="5dp"
        app:foregroundColor="@color/progress_btn_foreground_color"
        app:foregroundTextColor="@color/progress_btn_text_foreground"
        app:text="下载"
        app:textSize="12sp"/>

</LinearLayout>
